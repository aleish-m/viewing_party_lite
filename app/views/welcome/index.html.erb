<div id="page-title">
  <% if session[:user_id].nil? %>
    <center>
      <%= button_to 'Log In', login_path, method: :get  %>
      <br>
      <%= button_to 'Create an Account', register_path, method: :get  %>
      <br>
      <%= button_to 'Guest Log In', login_path, method: :post, params: {email: 'test1@mail.com', password: 'test123'} %>
      <br>
    </center>
  <% else %>
    <center>
      <br>
      <%= button_to 'Log Out', login_path, method: :delete  %>
      <br>
    </center>
  <% end %>
</div>

<% if session[:user_id] %>
  <center>
    <ul>
      <h3>Current Users</h3>
      <% @users.each_with_index do |user, index| %>
        <li id="user-<%= index %>">
          <p><%= user.name %>: <%= user.email%></p>
        </li>
      <% end %>
    </ul >
  </center>
<% end %>
